# ✅ Установка и первый запуск

Полная инструкция по установке и настройке системы управления контентом.

## 🚀 Этап 1: Установка зависимостей

### Шаг 1.1: Установите Node.js зависимости

```bash
npm install
```

Это установит все необходимые пакеты, включая новый `cheerio` для parsing HTML.

**Проверьте установку:**
```bash
npm list cheerio
# Должно вывести версию cheerio
```

---

## 🗄️ Этап 2: Подготовка базы данных

### Шаг 2.1: Создайте таблицы для контента

**Вариант A: Через PostgreSQL CLI**

```bash
# Убедитесь что PostgreSQL запущен
psql -U postgres

# Подключитесь к вашей БД
\c serverreport

# Выполните миграцию
\i migrations/001_create_content_table.sql

# Проверьте что таблицы созданы
\dt page_content
```

**Вариант B: Через pgAdmin или DBeaver**

1. Откройте ваш клиент БД
2. Откройте редактор SQL
3. Скопируйте содержимое `migrations/001_create_content_table.sql`
4. Выполните запрос

**Вариант C: Через Node.js (скрипт)**

```javascript
// Создайте файл initDb.js
const fs = require('fs');
const pool = require('./db');

async function runMigration() {
  try {
    const migration = fs.readFileSync('./migrations/001_create_content_table.sql', 'utf-8');
    await pool.query(migration);
    console.log('✓ Миграция выполнена успешно');
  } catch (error) {
    console.error('✗ Ошибка миграции:', error);
    process.exit(1);
  }
}

runMigration();
```

Потом выполните:
```bash
node initDb.js
```

---

## 📥 Этап 3: Загрузка HTML контента в БД

### Шаг 3.1: Запустите скрипт инициализации

```bash
npm run init-content
```

**Вывод должен выглядеть так:**
```
🔄 Начинаем инициализацию контента страниц...

✓ Таблица page_content найдена

✓ Загружена страница: index (15423 символов)
✓ Загружена страница: rules (8234 символов)
✓ Загружена страница: rules-overview (12456 символов)
✓ Загружена страница: dashboard (5432 символов)
✓ Загружена страница: rules-glossary (9876 символов)

✓ Инициализация контента завершена!
📊 Всего страниц в БД: 5
```

### Шаг 3.2: Проверьте загрузку

```bash
# Осмотрите БД напрямую
psql -U postgres -d serverreport -c "SELECT page_name, version FROM page_content;"
```

Должен вывести все загруженные страницы.

---

## 🎯 Этап 4: Интеграция в HTML страницы

### Шаг 4.1: Добавьте скрипт загрузки контента

Откройте ваш основной HTML файл (например, `rules-glossary.html`):

```html
<!DOCTYPE html>
<html>
<head>
    <!-- ... остальные теги ... -->
</head>
<body>
    <!-- ... ваше содержимое ... -->
    
    <!-- Добавьте перед </body> -->
    <script src="/content-manager.js"></script>
</body>
</html>
```

**Что происходит:**
- Скрипт автоматически получит имя страницы
- Загрузит контент из БД
- Заменит содержимое страницы

### Шаг 4.2: Повторите для всех страниц

Добавьте строку `<script src="/content-manager.js"></script>` перед `</body>` на всех отож HTML файлах, которые вы хотите интегрировать.

**Файлы для обновления:**
- `public/index.html`
- `public/rules.html`
- `public/rules-overview.html`
- `public/rules-guide.html`
- `public/rules-glossary.html`
- `public/dashboard.html`

---

## 🧪 Этап 5: Запуск и тестирование

### Шаг 5.1: Запустите сервер

```bash
npm start
```

Должны увидеть:
```
✓ Подключение к базе данных PostgreSQL установлено
🔄 Инициализация системы загрузки контента...
```

### Шаг 5.2: Откройте в браузере

1. **Проверьте что контент загружается:**
   ```
   http://localhost:3000/rules-glossary.html
   ```

2. **Откройте консоль браузера (F12):**
   Должны видеть логи типа:
   ```
   📖 Инициализация системы загрузки контента...
   📄 Текущая страница: rules-glossary
   🔄 Загужаю контент для rules-glossary...
   ✓ Контент rules-glossary успешно загружен
   ```

3. **Проверьте что title обновился:**
   - Откройте источник страницы (Ctrl+U)
   - Заголовок в броузере должен быть из БД

### Шаг 5.3: Проверьте API

```bash
# Получить контент API
curl http://localhost:3000/api/content/rules-glossary | jq

# Должен вывести JSON с контентом
```

---

## 👨‍💼 Этап 6: Настройка админ-панели

### Шаг 6.1: Откройте редактор контента

```
http://localhost:3000/content-manager-demo.html
```

**Если видите список страниц - всё работает! ✓**

### Шаг 6.2: Интегрируйте с вашей админ-панелью

Добавьте на вашу админ-страницу:

```html
<script src="/content-editor.js"></script>
<script>
  const editor = new ContentEditor({ isAdmin: true });
  
  // Получить все страницы
  editor.getAllPages().then(pages => {
    console.log('Страницы:', pages);
  });
</script>
```

---

## 📊 Проверочный список

Убедитесь что вы завершили все пункты:

- [ ] Установили `npm install`
- [ ] Выполнили миграцию БД (`migrations/001_create_content_table.sql`)
- [ ] Запустили `npm run init-content` успешно
- [ ] Добавили `<script src="/content-manager.js"></script>` в HTML файлы
- [ ] Запустили сервер `npm start`
- [ ] Видите логи загрузки контента в консоли браузера
- [ ] Открыли `content-manager-demo.html` и видите список страниц
- [ ] Успешно получаете контент через API

---

## 🔧 Конфигурация

### Переменные окружения (.env)

```env
# Основные
PORT=3000
NODE_ENV=production

# База данных (ОБЯЗАТЕЛЬНО)
DATABASE_URL=postgresql://user:password@localhost:5432/databasename

# JWT
JWT_SECRET=your_secret_key_minimum_32_characters
JWT_EXPIRES_IN=7d

# CORS (опционально)
CORS_ORIGIN=*
```

### Опции PageContentManager

```javascript
const manager = new PageContentManager({
  apiBaseUrl: '/api/content',     // Базовый URL API (по умолчанию)
  cacheEnabled: true,              // Включить кэширование (по умолчанию true)
});
```

### Опции ContentEditor

```javascript
const editor = new ContentEditor({
  apiBaseUrl: '/api/content',      // Базовый URL API (по умолчанию)
  isAdmin: true                     // Требуется для всех операций редактирования
});
```

---

## 🐛 Частые проблемы и решения

### ❌ "Таблица page_content не существует"

**Решение:** Выполните миграцию БД еще раз:
```bash
psql -U postgres -d serverreport < migrations/001_create_content_table.sql
```

### ❌ "cheerio is not defined"

**Решение:** Установите зависимость:
```bash
npm install cheerio
```

### ❌ "Контент не загружается на странице"

**Решение:** 
1. Проверьте консоль браузера (F12)
2. Убедитесь что скрипт подключен: `<script src="/content-manager.js"></script>`
3. Проверьте что страница в БД: 
   ```sql
   SELECT * FROM page_content WHERE page_name = 'rules-glossary';
   ```

### ❌ "401 Unauthorized" при редактировании

**Решение:** Убедитесь что:
1. Вы авторизованы (есть токен в localStorage)
2. Токен не истек
3. Пользователь имеет роль `admin`

### ❌ "ECONNREFUSED" на подключение БД

**Решение:**
1. Убедитесь что PostgreSQL запущен
2. Проверьте `DATABASE_URL` в `.env`
3. Проверьте имя БД, пользователя, пароля

---

## 📞 Справка

**Быстрые команды:**

```bash
# Установка
npm install

# Инициализация контента
npm run init-content

# Запуск сервера
npm start

# Проверка здоровья сервера
curl http://localhost:3000/health

# Проверка контента в БД
curl http://localhost:3000/api/content/rules-glossary
```

---

## ✨ Готово!

Поздравляем! Ваша система управления контентом полностью настроена и готова к использованию. 🎉

**Следующие шаги:**
- 📖 Прочитайте [полную документацию](./CONTENT-SYSTEM.md)
- 🔧 Посмотрите [примеры интеграции](./EXAMPLES.md)
- 📚 Изучите [API документацию](./API-DOCS.md)
- 🚀 Начните редактировать контент через админ-панель

**Помощь:**
- Если возникли проблемы - проверьте раздел "Частые проблемы"
- Все логи находятся в консоли браузера (F12) и консоли сервера
