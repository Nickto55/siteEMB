# 🚀 Быстрый старт - Система управления контентом

Следуйте этим шагам для настройки и запуска системы динамической загрузки контента.

## 📋 Шаг 1: Установка зависимостей

```bash
npm install
```

## 🗄️ Шаг 2: Создание таблиц в БД

Выполните миграцию SQL. У вас есть 2 варианта:

### Вариант A: Через psql (командная строка)
```bash
psql -U your_user -d your_database < migrations/001_create_content_table.sql
```

### Вариант B: Через графический клиент
1. Откройте pgAdmin или другой клиент PostgreSQL
2. Откройте вкладку SQL
3. Скопируйте содержимое файла `migrations/001_create_content_table.sql`
4. Выполните запрос

## 📥 Шаг 3: Загрузка содержимого HTML файлов в БД

```bash
npm run init-content
```

Должны вы увидеть что-то подобное:
```
🔄 Начинаем инициализацию контента страниц...

✓ Таблица page_content найдена

✓ Загружена страница: index (15423 символов)
✓ Загружена страница: rules (8234 символов)
✓ Загружена страница: rules-overview (12456 символов)
✓ Загружена страница: rules-guide (18923 символов)
✓ Загружена страница: rules-glossary (9876 символов)
✓ Загружена страница: dashboard (5432 символов)

✓ Инициализация контента завершена!
📊 Всего страниц в БД: 6
```

## 🎯 Шаг 4: Добавление системы загрузки контента на ваши страницы

Добавьте одну строку перед закрытием тега `</body>` в ваших HTML файлах:

```html
<!DOCTYPE html>
<html>
<head>
    <!-- ... ваш код ... -->
</head>
<body>
    <!-- ... ваше содержимое ... -->
    
    <!-- Добавьте эту строку перед </body> -->
    <script src="content-manager.js"></script>
</body>
</html>
```

**Что произойдет:**
- ✓ Скрипт автоматически определит название текущей страницы
- ✓ Загрузит содержимое из БД
- ✓ Заменит содержимое страницы на содержимое из БД
- ✓ Обновит заголовок страницы (`<title>`)

## 🔐 Шаг 5 (опционально): Добавить редактор для администраторов

Создайте новый HTML файл для админ-панели редактирования контента:

```html
<!DOCTYPE html>
<html>
<head>
    <title>Администратор - Управление контентом</title>
    <meta charset="UTF-8">
    <style>
        body {
            background: #f0f0f0;
            font-family: Arial, sans-serif;
            padding: 20px;
        }
        h1 { color: #333; }
        button {
            background: #5D8C30;
            color: white;
            padding: 10px 20px;
            border: none;
            border-radius: 4px;
            cursor: pointer;
        }
    </style>
</head>
<body>
    <h1>Управление контентом</h1>
    <div id="pages-list"></div>
    
    <script src="content-manager.js"></script>
    <script src="content-editor.js"></script>
    <script>
        const editor = new ContentEditor({ isAdmin: true });
        
        // Загрузить и показать все страницы
        async function showPages() {
            const pages = await editor.getAllPages();
            const html = pages.map(p => `
                <div>
                    <h3>${p.page_name}</h3>
                    <button onclick="editPage('${p.page_name}')">Редактировать</button>
                </div>
            `).join('');
            document.getElementById('pages-list').innerHTML = html;
        }
        
        async function editPage(pageName) {
            const data = await editor.getPageForEditing(pageName);
            // Откройте редактор с полученными данными
            console.log('Редактируем:', data);
        }
        
        showPages();
    </script>
</body>
</html>
```

## 🧪 Шаг 6: Проверка работы

1. **Запустите сервер:**
   ```bash
   npm start
   ```

2. **Откройте в браузере:**
   - Основной сайт: `http://localhost:3000`
   - Редактор администратора: `http://localhost:3000/content-manager-demo.html`

3. **Проверьте консоль браузера (F12):**
   - Должны быть логи типа: `✓ Контент rules-glossary успешно загружен`

4. **Проверьте БД:**
   ```sql
   SELECT page_name, version, updated_at FROM page_content;
   ```
   Должны быть видны все загруженные страницы.

## 🎨 Особенности загруемого контента

### Автоматическое обновление
- Каждый раз при загрузке страницы будет загружена **последняя версия** контента из БД
- Содержимое кэшируется локально, поэтому нет лишних запросов

### Полная история версий
- Все изменения сохраняются в таблице `page_content_history`
- Можно просмотреть кто и когда изменял контент

### Безопасность
- Редактирование требует авторизации и прав администратора
- Чтение контента публичное (не требует авторизации)

## 📊 API Endpoints (для разработки)

```bash
# Получить контент страницы
curl http://localhost:3000/api/content/rules-glossary

# Обновить контент (требует токен)
curl -X PUT http://localhost:3000/api/content/rules-glossary \
  -H "Authorization: Bearer YOUR_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"title":"Новый заголовок","content":"<html>...</html>"}'

# Получить все страницы (админы)
curl http://localhost:3000/api/content/admin/all \
  -H "Authorization: Bearer YOUR_TOKEN"
```

## 🐛 Если что-то не работает

### Контент не загружается
- [ ] Проверить что БД таблица создана: `SELECT * FROM page_content;`
- [ ] Проверить что контент загружен: `SELECT COUNT(*) FROM page_content;`
- [ ] Посмотреть консоль браузера (F12) на ошибки
- [ ] Проверить что скрипт `content-manager.js` подключен

### Скрипт инициализации не работает
- [ ] Проверить что cheerio установлен: `npm list cheerio`
- [ ] Проверить что `.env` файл настроен правильно
- [ ] Убедиться что папка `public` содержит HTML файлы

### БД ошибка: "Отношение не существует"
- [ ] Убедиться что миграция SQL была выполнена полностью
- [ ] Проверить что вы подключены к правильной БД
- [ ] Выполнить миграцию еще раз

## 📚 Дальнейшие ссылки

- [Полная документация системы](./CONTENT-SYSTEM.md)
- [API Reference](./CONTENT-SYSTEM.md#-api-endpoints)
- [Примеры использования](./CONTENT-SYSTEM.md#-примеры-использования)

## ✅ Готово!

Теперь ваша система полностью настроена и готова к использованию! 🎉

Все HTML страницы будут автоматически загружаться из БД вместо статической загрузки с диска.
